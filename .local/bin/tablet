#!/bin/sh

kill_huion(){
	if [ -n "$(pgrep 'huion')" ]; then
		killall huionCore
	else
		echo "No process found"
	fi
}

start_huion(){ /usr/lib/huiontablet/huionCore.sh > /dev/null 2>&1 & }

case "$1" in
	kill | k ) kill_huion ;;
	* ) kill_huion && start_huion ;;
esac
