#!/bin/sh

URL='https://duckduckgo.com/?q='
HISTORY="$HOME"/.cache/searchhis

if [ -z "$@" ]; then
	[ -f $HISTORY ] && cat $HISTORY
else
	! grep -qw "^$@" $HISTORY && echo "$@" >> $HISTORY
	$BROWSER "${URL}${@}" > /dev/null 2>&1 &
fi
