#!/bin/sh

if $WSL; then
	pgm='clip.exe'
elif $IS_WAYLAND; then
	pgm='wl-copy'
else
	pgm='xclip -sel c'
fi

if [ -p /dev/stdin ]; then
	cat /dev/stdin | $pgm
else
	case "$1" in
		v ) shift; echo $@ | $pgm ;;
		* ) cat $@ | $pgm ;;
	esac
fi
