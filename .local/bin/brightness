#!/bin/bash

app="xbacklight"
getB(){ $app -get; }
notify(){ dunstify -h string:x-dunst-stack-tag:brightness -i $HOME/.local/share/icons/general/brightness.svg "Brightness: $(getB)" -h int:value:$(getB); }

case "$1" in
	-u) $app -inc 2 && notify ;;
	-d) $app -dec 2 && notify ;;
esac
