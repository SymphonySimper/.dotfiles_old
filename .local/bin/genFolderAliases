#!/bin/sh

path="$HOME/.local/bin/.paths/folders"
folderAlias="$HOME/.config/shell/folders"

cat $path | sort -o $path && awk '{print "alias "$1"=\"cd "$2"\"" }' $path >  $folderAlias  &&
	dunstify "Folders aliases gen complete"
