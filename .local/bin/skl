#!/bin/sh

file="$XDG_CACHE_HOME/sl"

[ -f $file ] && layout=$(cat $file) || touch $file

cl='colemak_dh'
qw='qwerty'

case "$layout" in
	'colemak_dh' ) setxkbmap us && echo $qw > $file ;;
	* ) setxkbmap us -variant $cl && echo $cl > $file ;;
esac
