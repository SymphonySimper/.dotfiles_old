#!/bin/sh

loc="$HOME"/importantnt/audio/.lofi

is_lofi="$(pgrep -la "$VIDEOPLAYER" | grep 'title=lofi' | cut -d ' ' -f 1)"

if [ -n "$is_lofi" ]; then
	kill "$is_lofi"
else
	"$VIDEOPLAYER" --title='lofi' --volume=50 --loop-playlist --shuffle "$loc"
fi
