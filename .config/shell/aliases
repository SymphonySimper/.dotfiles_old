# Git alias
alias g='git'
alias gc='git clone'
alias gs='git status -v'
alias gp='git push'
alias gpu='git pull'
alias gau='git add -u'
alias gm='git commit'
alias gnotrack='git config --local status.showUntrackedFiles no'

# Config git alias
alias cgs='config status'
alias cgm='config commit'
alias cgp='config push'
alias cgau='config add -u'

# Package Manager
alias e='$PERM emerge -av'
alias rm_zombies='e --depclean'
alias update='$PERM emerge -q --sync && e --update --deep --with-bdeps=y @world'
alias am_i_fat='equery list "*" | wc -l'
alias erm='e -c'
alias newuse='e --update --deep --newuse @world'
alias changed_use='e --changed-use --deep @world'
alias elist='equery list "*" | less'

# Program
alias ls='ls --color=auto'
alias l='exa'
alias vs='$PERM nvim'
alias grep='grep --color=auto'
alias sudo='$PERM'
alias doas='$PERM'
alias less='less -Q'
alias flex='neofetch'
alias ytf='ytfzf -t'

# Tasks
alias rm_cache='rm -rf ~/.cache/*'
alias rm_me='rm -rf ~/.local/share/Trash/*'
alias rm_links='find . -xtype l -exec rm {} \+'
alias q='exit'
alias im_light='ps_mem -p $(pgrep -d, -u $USER)'
alias nobita='mntdev -n sdb1'
alias ka='killall'
alias bye='$PERM poweroff'
alias bbak='$PERM reboot'
alias temp='watch sensors'

# Shell History
alias his='$EDITOR $HISTFILE'
alias huh='eval "$(tac $HISTFILE | grep -v "q\|bye\|bbak\|exit\|clear" | fzf)"'

# Navigate dir
alias ..='cd ..'
alias j='cd "$(ls -aF | grep / | sed "s/\///" | fzf)"'
alias jj='cd ~$(dirs -v | fzf | cut -f 1)'
alias cdp='cd /etc/portage'
alias cdk='cd /usr/src/linux'

# Open files in download folders with v
alias vdd='v -x "$HOME"/dl'

# Stuff
alias hex_a='less "$HOME"/.local/share/my-docs/hex-alpha'
alias todo='$EDITOR "$HOME"/.local/share/.todo'
alias begone='$EDITOR "$HOME"/.local/share/.begone'
alias rb='devour $READER "$HOME"/lifeisfun/rust/rust-lang.pdf'
alias kd='$EDITOR "$HOME"/.local/share/my-docs/compile-kernel'
alias hosts='$PERM $EDITOR /etc/hosts'

# Clean up
alias yarn='yarn --use-yarnrc "$XDG_CONFIG_HOME/yarn/config"'
alias wget='wget --hsts-file="$XDG_CACHE_HOME/wget-hsts"'
alias nvidia-settings='nvidia-settings --config="$XDG_CONFIG_HOME"/nvidia/settings'
